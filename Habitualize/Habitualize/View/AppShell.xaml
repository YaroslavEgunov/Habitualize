<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Habitualize"
             x:Class="Habitualize.View.AppShell"
             xmlns:pages="clr-namespace:Habitualize.SignPages"
             Title="AppShell"
             Shell.FlyoutBehavior="Disabled">
    <ShellContent
        ContentTemplate="{DataTemplate pages:SignInView}"
        Route="SignIn" />

    <ShellContent
        ContentTemplate="{DataTemplate pages:SignUpView}"
        Route="SignUp" />

    <ShellContent  
        ContentTemplate="{DataTemplate local:MainPage}" 
        Route="MainPage"/>

</Shell>

